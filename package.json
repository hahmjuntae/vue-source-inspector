{
  "name": "vue-source-inspector-extension",
  "private": true,
  "version": "1.0.2",
  "license": "UNLICENSED",
  "description": "Chrome DevTools extension for tracing Vue DOM nodes to source files and related styles.",
  "scripts": {
    "lint": "node scripts/verify.js",
    "static": "node scripts/verify.js",
    "test": "node --test"
  }
}
